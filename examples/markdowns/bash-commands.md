# This would run a shell through bash with multiple commands
```shell bash
echo "ls -all command" 
ls -all
echo "cd .. command"
cd ..
echo "ls -all command again"
ls -all 
echo "ps -aux command"
ps -aux
echo "exit command"
exit 
```
